apiVersion: apps/v1
kind: Deployment
metadata:
  name: ffmpeg
spec:
  replicas: 4
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app: ffmpeg-server
  template:
    metadata:
      labels:
        app: ffmpeg-server
    spec:
      containers: 
      - image: linuxserver/ffmpeg:arm64v8-version-4.4-cli
        name: ffmpeg-se
        ports:
        - containerPort: 8080

